function isEmpty() { }

for (const obj of isEmpty) {
    let schedule = "";
    if (schedule === "") {
        alert(isEmpty(schedule));
    }
    else {
        alert(isEmpty(schedule));
    }
}

isEmpty(obj);

